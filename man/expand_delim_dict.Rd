% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand_delim_dict.R
\name{expand_delim_dict}
\alias{expand_delim_dict}
\title{Expand a delimited data dictionary}
\usage{
expand_delim_dict(
  data,
  var_col,
  from_col,
  to_col,
  from_delim = ",",
  to_delim = ";",
  remove_na = FALSE,
  remove_empty = FALSE,
  run_check = TRUE,
  min_rows = 1L,
  quiet = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble containing the unique \code{from} and \code{to}
values for variables in a dataset.}

\item{var_col}{A character string of the name of the column in \code{data}
that contains unique variable names in your dataset.}

\item{from_col}{A character string specifying the name of the column in
\code{data} that contains the original values (i.e., \verb{from values}) for each
variable in your dataset.}

\item{to_col}{A character string specifying the name of the column in
\code{data} that contains the new values (i.e., \verb{to values}) for each variable
in your dataset.}

\item{from_delim}{A character or set of characters used to separate values
in the \code{from_col}. Default is a comma \verb{,}.}

\item{to_delim}{A character or set of characters used to separate values
in the \code{to_col}. Default is a semi-colon \verb{;}.}

\item{remove_na}{A logical value indicating whether to remove \code{NA}
values from both \code{from_col} and \code{to_col.} Default is \code{FALSE}.}

\item{remove_empty}{A logical value indicating whether to remove empty
strings (including entries that are blank or contain only whitespace)
from both \code{from_col} and \code{to_col}. Default is \code{FALSE}.}

\item{run_check}{A logical value indicating whether to run
\code{\link{validate_dict}} to validate the expanded data dictionary.

Verification ensures:
\itemize{
\item Each unique category in \code{var_col} meets the minimum row threshold.
\item A strict one-to-one relationship exists between \code{from} and \code{to}
values for every unique category in \code{var_col}.
}
Default is \code{TRUE}.}

\item{min_rows}{A non-negative integer indicating the minimum number
of entries (rows) expected for each variable in the data dictionary.
Default is \code{1}.}

\item{quiet}{A logical value indicating whether to suppress progress
and validation success alerts. Default is \code{FALSE}.}
}
\value{
A tibble in long format containing only the three columns specified
in \code{var_col}, \code{from_col}, and \code{to_col}.
}
\description{
\code{expand_delim_dict()} unpacks a delimited data dictionary
by expanding rows containing multiple values into a tidy, row-per-value
format. The function identifies entries where multiple original values and
labels are stored as strings (e.g., "1,2,3") and restructures the data so
that each unique \code{from}/\code{to} pair occupies its own dedicated row.

\strong{Note:} The function only retains columns relevant to the expansion
process: \code{var_col}, \code{from_col}, and \code{to_col.} All other columns present in
the input dictionary are removed during processing.
}
\examples{
expand_delim_dict(
  data = grad_app_dict_delim,
  var_col = "column_name",
  from_col = "old_values",
  to_col = "new_labels",
  from_delim = ";",
  to_delim = ";",
  remove_na = FALSE,
  remove_empty = FALSE,
  min_rows = 1L
)

}
\author{
Ama Nyame-Mensah
}
